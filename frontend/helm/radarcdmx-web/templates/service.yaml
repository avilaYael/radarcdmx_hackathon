apiVersion: v1
kind: Service
metadata:
  name: {{ include "radarcdmx-web.fullname" . }}
  labels:
    {{- include "radarcdmx-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "radarcdmx-web.selectorLabels" . | nindent 4 }}
